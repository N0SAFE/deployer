'use client'

// This component is no longer needed since we're using Sonner for notifications
// Keeping it empty for backward compatibility with existing imports
export function NotificationToasts() {
  return null
}