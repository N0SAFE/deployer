---
title: Entity & Model Hierarchy
description: Canonical map of v3 entity families, relationships, mesh model layers, and contract/runtime boundaries.
order: 4
---

> **Added**: 2026-04-16  
> **Type**: Reference  
> **Confidence**: Verified ✅  
> **Scope**: v3 architecture

This page documents the **actual model hierarchy** of the v3 platform from source-of-truth schema files, including:

- business entities (`project`, `service`, `deployment`, `user`)
- infrastructure entities (`docker/*`, `traefik`, `setup`)
- orchestration/control entities (`event-stream`, `mesh/*`)
- API contract and runtime wiring (`contracts/api` → `apps/api` → `apps/web`)

---

## 1) Source-of-truth layering

```mermaid
flowchart TD
    A[packages/contracts/entities\nZod entity schemas] --> B[packages/contracts/api\nORPC contract modules]
    B --> C[apps/api\nNestJS controller/service/runtime]
    B --> D[apps/web\ndomain endpoints/hooks]
    C --> E[(PostgreSQL + runtime integrations)]
```

Interpretation:

1. `contracts/entities` defines model vocabulary.
2. `contracts/api` exposes typed operations over those models.
3. API/web consume the contracts as implementation and client entry points.

---

## 2) Global entity family hierarchy

```mermaid
mindmap
  root((contracts/entities))
    user
      user/base.schema.ts
    project
      core.schema.ts
      settings.schema.ts
      environments.schema.ts
      collaborators.schema.ts
      templates.schema.ts
    service
      service.schema.ts
      provider-config.schema.ts
      runner-config.schema.ts
    deployment
      base.schema.ts
      plan.schema.ts
      queue.schema.ts
      retry.schema.ts
      phase.schema.ts
      execution.schema.ts
      lifecycle-events.schema.ts
      provenance.schema.ts
    docker
      containers/*
      images/*
      networks/*
      volumes/*
      registries/*
      stacks/*
      runtime.schema.ts
      security/scanning/*
    mesh
      topology.schema.ts
      control.schema.ts
      routing-resources.schema.ts
      trust.schema.ts
      duplex.schema.ts
    event-stream
      base.schema.ts
      envelopes.schema.ts
    template
      vocabulary/config/templates/validation/resolver/migration
    configuration
      service-config.schema.ts
      dependency-targeting.schema.ts
      service-overrides.schema.ts
      project-environment.schema.ts
    setup
      state.schema.ts
      initialize.schema.ts
      database.schema.ts
      node.schema.ts
    traefik
      rules.schema.ts
      dynamic-config.schema.ts
```

---

## 3) Core business model graph

```mermaid
classDiagram
    class User {
      id
      email
      role
    }
    class Project {
      id
      ownerId
      settings
      baseDomain
    }
    class Service {
      id
      projectId
      providerId/providerConfig
      builderId/builderConfig
      traefikConfig
    }
    class Deployment {
      id
      serviceId
      status
      environment
      sourceType/sourceConfig
      progress/observability
    }
    class DeploymentPlan {
      nodes[]
      edges[]
      rollbackGraph
    }
    class ProjectSettings {
      general/deployment/security
      resources/notifications
      environment presets
    }

    User "1" --> "many" Project : ownerId
    Project "1" --> "many" Service : projectId
    Service "1" --> "many" Deployment : serviceId
    Deployment "1" --> "0..1" DeploymentPlan : compiled snapshot
    Project "1" --> "1" ProjectSettings : settings
```

---

## 4) Docker entity relationship model (`z.lazy` graph)

`docker/*/relations.schema.ts` defines relation-bearing entity envelopes and uses `z.lazy(...)` extensively for linked graphs.

```mermaid
flowchart LR
    C[DockerContainerEntity] --> I[DockerImageEntity]
    C --> N[DockerNetworkEntity]
    C --> V[DockerVolumeEntity]
    C --> ST[DockerStackEntity]
    C --> R[DockerRegistryEntity]
    C --> SVC[ServiceSnapshot]
    C --> DEP[DeploymentSnapshot]
    C --> PROJ[ProjectRef]

    I --> R
    I --> C
    I --> N
    I --> V
    I --> ST
    I --> DEP
    I --> SVC
    I --> PROJ
    I --> ICH[Image parent/children]

    N --> C
    N --> SVC
    N --> PROJ
    N --> ST

    V --> C
    V --> SVC
    V --> PROJ
    V --> ST

    ST --> C
    ST --> I
    ST --> N
    ST --> V
    ST --> R
```

And the runtime aggregate is explicitly modeled by `dockerRuntimeCatalogSchema`:

```mermaid
flowchart TD
    RC["DockerRuntimeCatalog"]
    RC --> RCC["containers: DockerContainerEntity[]"]
    RC --> RCI["images: DockerImageEntity[]"]
    RC --> RCN["networks: DockerNetworkEntity[]"]
    RC --> RCV["volumes: DockerVolumeEntity[]"]
    RC --> RCR["registries: DockerRegistryEntity[]"]
    RC --> RCS["stacks: DockerStackEntity[]"]
```

---

## 5) Mesh model hierarchy (entities)

The mesh model is split into five cohesive layers:

```mermaid
flowchart TD
    M[mesh/* entities]
    M --> T[topology.schema.ts]
    M --> C[control.schema.ts]
    M --> R[routing-resources.schema.ts]
    M --> TR[trust.schema.ts]
    M --> D[duplex.schema.ts]

    T --> T1[Node/Peer/Session/Membership]
    T --> T2[Runtime + Topology events]

    C --> C1[Control envelope + typed event transport]

    R --> R1[Resource lookup/index]
    R --> R2[Stream route planning]
    R --> R3[Queue partition + transition log]

    TR --> TR1[Join grants + enrollment]
    TR --> TR2[Keyring + strict mode rollout/rollback]

    D --> D1[Duplex stream input union]
    D --> D2[Duplex stream output union]
    D --> D3[Auth/heartbeat/control/reconcile/snapshot]
```

---

## 6) Mesh contract hierarchy (`core.mesh`)

`packages/contracts/api/modules/core/index.ts` mounts `meshContract` under `core.mesh`.

```mermaid
flowchart TD
    APP[appContract] --> CORE[coreContract /core]
    CORE --> MESH[meshContract /mesh]
    CORE --> FLEET[fleetContract /fleet]

    MESH --> M1[node + peers + sessions]
    MESH --> M2[event streams + subscribe]
    MESH --> M3[topology/runtime streaming]
    MESH --> M4[control envelope publish]
    MESH --> M5[resource lookup/index]
    MESH --> M6[queue partition planning]
    MESH --> M7[enrollment join grants]
    MESH --> M8[trust keyring + strict mode]
    MESH --> M9[duplex session stream]
```

---

## 7) Mesh runtime wiring in API

```mermaid
flowchart LR
    C1[SystemMeshController\napps/api/src/system/modules/mesh/controllers] --> S1[SystemMeshTopologyService]
    C1 --> S2[CoreEventSyncService]
    C1 --> S3[SystemMetricsService]

    S1 --> MC[MeshCoreModule]
    MC --> E1[SystemMeshEventService]
    MC --> E2[SystemMeshTopicService]
    MC --> E3[MeshStreamRuntimeService]
    MC --> E4[SystemMeshResourceDiscoveryService]
    MC --> E5[MeshQueueTransitionService]
    MC --> E6[MeshInternalRequestService]
    MC --> R1[SystemMeshClusterRepository]
    MC --> R2[NodeMeshConfigRepository]
```

### Internal domain-mesh service pattern

`BaseMeshService` provides internal event-contract based call/observe semantics:

```mermaid
sequenceDiagram
    participant DS as Domain Mesh Service (extends BaseMeshService)
    participant TS as SystemMeshTopicService
    participant TOPO as SystemMeshTopologyService
    participant PEER as Peer Node Service

    DS->>TS: registerNamespace(contracts)
    DS->>TS: publish(request topic, correlationId)
    TS-->>PEER: request event
    PEER-->>TS: response event (optional stopPropagation)
    TS-->>DS: observe response stream
    DS->>TS: publish cancel envelope (kill-switch when needed)
    DS->>TOPO: listPeerSessions()/local node context
```

---

## 8) End-to-end model consumption path (web ↔ contracts ↔ api)

```mermaid
flowchart LR
    W[apps/web/src/domains/*/endpoints.ts] --> O[orpc client]
    O --> A[packages/contracts/api appContract]
    A --> N[apps/api controllers via @Implement + implement]
    N --> SV[services]
    SV --> DB[(Postgres/infra)]
```

Examples of domain endpoint maps already aligned to this shape:

- `apps/web/src/domains/project/endpoints.ts`
- `apps/web/src/domains/service/endpoints.ts`
- `apps/web/src/domains/deployment/endpoints.ts`
- `apps/web/src/domains/docker/endpoints.ts`
- `apps/web/src/domains/mesh/endpoints.ts`

---

## 9) Contract module hierarchy (API surface)

```mermaid
flowchart TD
    AC[appContract]
    AC --> U[user]
    AC --> H[health]
    AC --> O[organization]
    AC --> P[project]
    AC --> S[service]
    AC --> D[deployment]
    AC --> DK[docker]
    AC --> TM[template]
    AC --> DM[domain]
    AC --> AN[analytics]
    AC --> ST[setup]
    AC --> C[core]
    C --> C1[mesh]
    C --> C2[fleet]
```

---

## 10) Source index (key files)

### Entity schemas

- `packages/contracts/entities/src/index.ts`
- `packages/contracts/entities/src/entities/project/*`
- `packages/contracts/entities/src/entities/service/*`
- `packages/contracts/entities/src/entities/deployment/*`
- `packages/contracts/entities/src/entities/docker/*`
- `packages/contracts/entities/src/entities/mesh/*`
- `packages/contracts/entities/src/entities/event-stream/*`
- `packages/contracts/entities/src/entities/template/*`
- `packages/contracts/entities/src/entities/configuration/*`
- `packages/contracts/entities/src/entities/setup/*`
- `packages/contracts/entities/src/entities/traefik/*`

### API contract wiring

- `packages/contracts/api/index.ts`
- `packages/contracts/api/modules/index.ts`
- `packages/contracts/api/modules/core/index.ts`
- `packages/contracts/api/modules/mesh/index.ts`

### API runtime wiring (mesh)

- `apps/api/src/core/modules/mesh/mesh-core.module.ts`
- `apps/api/src/system/modules/mesh/system-mesh.module.ts`
- `apps/api/src/system/modules/mesh/controllers/system-mesh.controller.ts`
- `apps/api/src/core/modules/mesh/services/base-mesh.service.ts`

### Web model consumers

- `apps/web/src/domains/*/endpoints.ts`

---

## 11) Practical reading order

1. Start with `contracts/entities` domain you need.
2. Move to matching `contracts/api/modules/<domain>`.
3. Read API controller + service implementation.
4. Confirm web domain endpoint/hook usage.
5. For distributed behavior, follow `core.mesh` + `BaseMeshService` first.

---

## 12) Mega ownership map (entire app in one diagram)

This is the single, end-to-end ownership and responsibility map you asked for.

```mermaid
flowchart TB
  %% =============================
  %% PLATFORM / INFRA LAYER
  %% =============================
  subgraph PLATFORM[Platform Cluster]
    direction TB

    subgraph SERVER_A[Server Node A]
      direction TB
      A_API[API Runtime\nNestJS + ORPC]
      A_WEB[Web Runtime\nNext.js]
      A_MESH[Mesh Node\nnodeId + roles]
      A_TRAEFIK[Traefik Instance\nrouters/services/middlewares]
      A_DOCKER[Docker Engine]
      A_LOCAL_DB[(Local DB\nSQLite / node config)]

      A_DOCKER --> A_CONT[Containers]
      A_DOCKER --> A_IMG[Images]
      A_DOCKER --> A_NET[Networks]
      A_DOCKER --> A_VOL[Volumes]
      A_DOCKER --> A_REG[Registries]
      A_DOCKER --> A_STK[Stacks]
      A_DOCKER --> A_RT[Runtime Streams\nlogs/processes/terminal/filesystem/events]
    end

    subgraph SERVER_B[Server Node B]
      direction TB
      B_API[API Runtime]
      B_MESH[Mesh Node]
      B_TRAEFIK[Traefik Instance]
      B_DOCKER[Docker Engine]
      B_LOCAL_DB[(Local DB\nSQLite)]
    end

    GLOBAL_DB[(Global DB\nPostgreSQL shared state)]
  end

  %% =============================
  %% IDENTITY / ACCESS LAYER
  %% =============================
  subgraph IAM[Identity, Membership & Roles]
    direction TB
    USER[User]

    subgraph PLATFORM_ROLES[Platform Roles\n@repo/auth PLATFORM_ROLES]
      P_SUPER[superAdmin]
      P_ADMIN[admin]
      P_OP[operator]
      P_VIEW[viewer]
    end

    ORG_MEMBERSHIP[Organization Membership]

    subgraph ORG_ROLES[Organization Roles\nORGANIZATION_ROLES]
      O_OWNER[owner]
      O_ADMIN[admin]
      O_MEMBER[member]
    end

    PROJECT_COLLAB[Project Collaborator]

    subgraph PROJECT_ROLES[Project Roles\nPROJECT_ROLES]
      PR_OWNER[owner]
      PR_MAINT[maintainer]
      PR_DEPLOY[deployer]
      PR_VIEW[viewer]
    end
  end

  %% =============================
  %% BUSINESS OWNERSHIP LAYER
  %% =============================
  subgraph BUSINESS[Business Ownership Chain]
    direction TB
    ORG[Organization]
    PROJECT[Project\nownerId + settings]
    SERVICE[Service\nproviderConfig + builderConfig]
    DEPLOYMENT[Deployment\nstatus/environment/source]
    DEP_PLAN[Deployment Compiled Plan\nnodes + edges + rollback graph]
    DEP_QUEUE[Deployment Queue / Execution]
    DEP_EVENTS[Deployment Lifecycle Events]
  end

  %% =============================
  %% DOMAIN / ROUTING / TEMPLATE LAYER
  %% =============================
  subgraph ROUTING[Template, Domain & Traffic Control]
    direction TB
    TEMPLATE[Templates\nprovider/build/deploy/route/preview/dependency]
    ORG_DOMAIN[Organization Domain]
    PROJ_DOMAIN[Project Domain]
    SVC_DOMAIN[Service Domain Mapping]
    TRAEFIK_CFG[Traefik Dynamic Config]
    ROUTE_RULES[Traefik Rules\nrouters/services/middlewares]
  end

  %% =============================
  %% DOCKER RESOURCE OWNERSHIP
  %% =============================
  subgraph DOCKER_MODEL[Docker Entity Ownership & Links]
    direction TB
    D_CONTAINER[Container Entity]
    D_IMAGE[Image Entity]
    D_NETWORK[Network Entity]
    D_VOLUME[Volume Entity]
    D_REGISTRY[Registry Entity]
    D_STACK[Stack Entity]
  end

  %% =============================
  %% MESH / DISTRIBUTED OWNERSHIP
  %% =============================
  subgraph MESH_MODEL[Mesh Ownership & Resource Index]
    direction TB
    M_NODE[Mesh Node State]
    M_PEER[Peer Connection / Session]
    M_RESOURCE[Resource Location Index\nkind+key -> ownerNodeId]
    M_STREAM[Stream Route Plan]
    M_CONTROL[Control Envelope]
    M_TRUST[Trust / Join Grants / Keyring]
    M_QUEUE[Queue Partition Ownership]
  end

  %% =============================
  %% EDGES: IAM -> BUSINESS
  %% =============================
  USER --> PLATFORM_ROLES
  USER --> ORG_MEMBERSHIP --> ORG_ROLES
  USER --> PROJECT_COLLAB --> PROJECT_ROLES

  USER --> ORG
  ORG --> PROJECT
  PROJECT --> SERVICE
  SERVICE --> DEPLOYMENT
  DEPLOYMENT --> DEP_PLAN
  DEPLOYMENT --> DEP_QUEUE
  DEPLOYMENT --> DEP_EVENTS

  %% =============================
  %% EDGES: BUSINESS -> ROUTING/TEMPLATE
  %% =============================
  TEMPLATE --> SERVICE
  ORG --> ORG_DOMAIN
  PROJECT --> PROJ_DOMAIN
  SERVICE --> SVC_DOMAIN
  SERVICE --> TRAEFIK_CFG --> ROUTE_RULES
  ORG_DOMAIN --> ROUTE_RULES
  PROJ_DOMAIN --> ROUTE_RULES
  SVC_DOMAIN --> ROUTE_RULES

  %% =============================
  %% EDGES: BUSINESS -> DOCKER MODEL
  %% =============================
  SERVICE --> D_CONTAINER
  DEPLOYMENT --> D_CONTAINER
  DEPLOYMENT --> D_IMAGE
  PROJECT --> D_CONTAINER
  PROJECT --> D_IMAGE
  PROJECT --> D_NETWORK
  PROJECT --> D_VOLUME
  PROJECT --> D_STACK

  D_CONTAINER --> D_IMAGE
  D_CONTAINER --> D_NETWORK
  D_CONTAINER --> D_VOLUME
  D_CONTAINER --> D_STACK
  D_CONTAINER --> D_REGISTRY

  D_IMAGE --> D_REGISTRY
  D_IMAGE --> D_STACK
  D_STACK --> D_NETWORK
  D_STACK --> D_VOLUME

  %% =============================
  %% EDGES: SERVER OWNS DOCKER FEATURES
  %% =============================
  A_API --> GLOBAL_DB
  A_API --> A_LOCAL_DB
  A_API --> A_TRAEFIK
  A_API --> A_DOCKER
  A_API --> A_MESH

  B_API --> GLOBAL_DB
  B_API --> B_LOCAL_DB
  B_API --> B_TRAEFIK
  B_API --> B_DOCKER
  B_API --> B_MESH

  %% Map abstract docker entities to server runtime ownership
  A_DOCKER --> D_CONTAINER
  A_DOCKER --> D_IMAGE
  A_DOCKER --> D_NETWORK
  A_DOCKER --> D_VOLUME
  A_DOCKER --> D_REGISTRY
  A_DOCKER --> D_STACK

  B_DOCKER --> D_CONTAINER
  B_DOCKER --> D_IMAGE
  B_DOCKER --> D_NETWORK
  B_DOCKER --> D_VOLUME
  B_DOCKER --> D_REGISTRY
  B_DOCKER --> D_STACK

  %% =============================
  %% EDGES: MESH DISTRIBUTED OWNERSHIP
  %% =============================
  A_MESH --> M_NODE
  B_MESH --> M_NODE
  M_NODE --> M_PEER
  M_CONTROL --> M_PEER
  M_RESOURCE --> M_STREAM
  M_STREAM --> M_QUEUE
  M_TRUST --> M_CONTROL

  %% Resource ownership ties to business/runtime entities
  M_RESOURCE --> DEPLOYMENT
  M_RESOURCE --> DEP_EVENTS
  M_RESOURCE --> D_CONTAINER
  M_RESOURCE --> D_STACK

  %% =============================
  %% EDGES: TRAEFIK + DB OWNERSHIP
  %% =============================
  ROUTE_RULES --> A_TRAEFIK
  ROUTE_RULES --> B_TRAEFIK
  GLOBAL_DB --> ORG
  GLOBAL_DB --> PROJECT
  GLOBAL_DB --> SERVICE
  GLOBAL_DB --> DEPLOYMENT
  GLOBAL_DB --> DEP_EVENTS
  GLOBAL_DB --> M_RESOURCE
  A_LOCAL_DB --> A_MESH
  B_LOCAL_DB --> B_MESH
```

### Reading notes for this mega graph

- **Global DB** = shared PostgreSQL truth (org/projects/services/deployments/events/mesh index).  
- **Local DB** = per-node SQLite config/state used by node-level runtime services.  
- **Server owns Docker features** at runtime (`containers/images/networks/volumes/registries/stacks` + streams).  
- **User ownership is layered**: platform role + organization membership role + project collaborator role.  
- **Mesh owns distributed routing decisions**, not business semantics (it routes ownership for streams/resources/partitions).

---

## 13) Alternate view: ER hierarchy map (link-first semantics)

This view uses an **ER diagram** so ownership and hierarchy are read directly from relationship links.

```mermaid
erDiagram
  USER ||--o{ ORG_MEMBERSHIP : "joins"
  ORGANIZATION ||--o{ ORG_MEMBERSHIP : "has_members"

  ORGANIZATION ||--o{ PROJECT : "owns"
  USER ||--o{ PROJECT : "ownerId"

  PROJECT ||--o{ PROJECT_COLLABORATOR : "grants_role"
  USER ||--o{ PROJECT_COLLABORATOR : "assigned"

  PROJECT ||--o{ SERVICE : "contains"
  SERVICE ||--o{ DEPLOYMENT : "releases"
  DEPLOYMENT ||--o| DEPLOYMENT_PLAN_SNAPSHOT : "compiled_as"
  DEPLOYMENT ||--o{ DEPLOYMENT_EVENT : "emits"
  DEPLOYMENT ||--o{ DEPLOYMENT_QUEUE_JOB : "queued_in"

  SERVICE ||--o{ SERVICE_DOMAIN_MAPPING : "mapped_to"
  PROJECT ||--o{ PROJECT_DOMAIN : "mapped_to"
  ORGANIZATION ||--o{ ORGANIZATION_DOMAIN : "mapped_to"

  SERVICE ||--o{ TRAEFIK_CONFIG : "renders"
  TRAEFIK_CONFIG ||--o{ TRAEFIK_ROUTE_RULE : "materializes"

  PROJECT ||--o{ TEMPLATE_BINDING : "uses_templates"
  TEMPLATE ||--o{ TEMPLATE_BINDING : "applied_to"

  SERVER_NODE ||--|| LOCAL_DB_SQLITE : "stores_local_config"
  SERVER_NODE ||--o{ DOCKER_CONTAINER : "hosts"
  SERVER_NODE ||--o{ DOCKER_IMAGE : "caches"
  SERVER_NODE ||--o{ DOCKER_NETWORK : "manages"
  SERVER_NODE ||--o{ DOCKER_VOLUME : "manages"
  SERVER_NODE ||--o{ DOCKER_REGISTRY_REF : "connects"
  SERVER_NODE ||--o{ DOCKER_STACK : "orchestrates"

  DOCKER_CONTAINER }o--|| DOCKER_IMAGE : "runs_from"
  DOCKER_CONTAINER }o--o{ DOCKER_NETWORK : "attached_to"
  DOCKER_CONTAINER }o--o{ DOCKER_VOLUME : "mounts"
  DOCKER_CONTAINER }o--o| DOCKER_STACK : "part_of"
  DOCKER_IMAGE }o--o| DOCKER_REGISTRY_REF : "pulled_from"

  GLOBAL_DB_POSTGRES ||--o{ ORGANIZATION : "source_of_truth"
  GLOBAL_DB_POSTGRES ||--o{ PROJECT : "source_of_truth"
  GLOBAL_DB_POSTGRES ||--o{ SERVICE : "source_of_truth"
  GLOBAL_DB_POSTGRES ||--o{ DEPLOYMENT : "source_of_truth"
  GLOBAL_DB_POSTGRES ||--o{ DEPLOYMENT_EVENT : "source_of_truth"

  MESH_NODE ||--o{ MESH_PEER_SESSION : "maintains"
  MESH_NODE ||--o{ MESH_RESOURCE_INDEX : "owns_resources"
  MESH_RESOURCE_INDEX }o--|| SERVICE : "routes_to"
  MESH_RESOURCE_INDEX }o--|| DEPLOYMENT : "routes_to"
  MESH_RESOURCE_INDEX }o--|| DOCKER_CONTAINER : "routes_to"
```

### How to read hierarchy from links

- `||--o{` means **one-to-many ownership** from left to right (parent owns many children).
- `}o--||` means **many-to-one dependency** from left to right (many children point to one parent).
- `||--||` means **one-to-one binding** (e.g., one server node → one local SQLite config DB).
- Relationship labels (`owns`, `contains`, `releases`, `runs_from`, `mapped_to`, etc.) express the hierarchy intent.

### Role hierarchy reference attached to this model

- Platform roles: `superAdmin` > `admin` > `operator` > `viewer`
- Organization roles: `owner` > `admin` > `member`
- Project roles: `owner` > `maintainer` > `deployer` > `viewer`

These role ladders govern access over the linked entities above (organization, project, service, deployment, domain, and operations).
