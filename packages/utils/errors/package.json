{
    "name": "@repo/errors",
    "version": "1.0.0",
    "description": "Canonical application error hierarchy. All custom errors across the monorepo should extend these base classes.",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "prettify": "prettier . --write --no-error-on-unmatched-pattern",
        "format": "bun run prettify",
        "format:check": "prettier . --check --no-error-on-unmatched-pattern",
        "lint": "eslint . --fix --cache --concurrency=auto",
        "type-check": "tsc --noEmit"
    },
    "devDependencies": {
        "@repo/config-eslint": "*",
        "@repo/config-prettier": "*",
        "@repo/config-typescript": "*",
        "@types/node": "catalog:build",
        "eslint": "catalog:linting",
        "typescript": "catalog:build"
    }
}
