{
    "name": "@repo/types",
    "version": "1.0.0",
    "exports": {
        "./*": {
            "import": {
                "types": "./*.ts"
            },
            "require": {
                "types": "./*.ts"
            }
        }
    },
    "scripts": {
        "prettify": "prettier . --write --no-error-on-unmatched-pattern",
        "format": "bun run prettify",
        "format:check": "prettier . --check --no-error-on-unmatched-pattern",
        "lint": "eslint \"**/*.ts*\"",
        "type-check": "tsgo --noEmit",
        "test": "vitest run",
        "test:watch": "vitest",
        "test:coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@types/node": "catalog:build",
        "eslint": "catalog:linting",
        "@eslint/eslintrc": "^3.3.3",
        "@eslint/js": "catalog:linting",
        "@repo/config-eslint": "*",
        "@repo/config-prettier": "*",
        "@repo/config-typescript": "*",
        "@repo/config-vitest": "*",
        "@typescript/native-preview": "catalog:build",
        "vitest": "catalog:testing",
        "zod": "catalog:utils",
        "drizzle-orm": "catalog:database"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "@repo/type-guards": "*",
        "concurrently": "^10.0.3",
        "rimraf": "catalog:build"
    },
    "peerDependencies": {
        "concurrently": "^10.0.3",
        "rimraf": "catalog:build"
    }
}
