{
  "name": "@repo/contracts-entities",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "type-check": "tsgo --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@repo/auth": "*",
    "@repo/contracts-common": "*",
    "@repo/orpc-utils": "*",
    "@repo/provider-schema": "*",
    "zod": "catalog:utils"
  },
  "devDependencies": {
    "@types/node": "catalog:build",
    "@repo/config-vitest": "*",
    "@repo/config-typescript": "*",
    "@vitest/coverage-v8": "catalog:testing",
    "@typescript/native-preview": "catalog:build",
    "vitest": "catalog:testing"
  }
}
